# Docs Index

Catalog of every page under `pagochealth-plc/docs/`. Maintained on every doc change. For workspace-wide knowledge (strategy, suppliers, cross-sub-system decisions), see the company wiki at `../pagochealth-wiki/`.

## Architecture

- [[architecture/overview|Architecture overview]] — OPTA ↔ GIGA block diagram and Modbus topology
- [[architecture/io-map|I/O map]] — every OPTA input/output and what it controls
- [[architecture/communication|Communication — Modbus TCP register map]] — authoritative tag definitions
- [[architecture/state-machine|Product state machine]] — fill / heat / cool / drain / idle states

## Components

### OPTA (control function blocks)

_Empty — populated as function blocks are added._

### HMI (screens)

_Empty — populated as HMI screens are added._

## Decisions (firmware-specific ADRs)

_Empty — populated as firmware-local architectural decisions are made._
